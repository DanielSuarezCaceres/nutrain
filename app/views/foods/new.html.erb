<div class="container p-3 col-6">
  <div class="card">
    <div class="card-header d-flex">
      <h2>New food</h2>
      <%= link_to 'Back', :back, class: 'btn ml-auto' %>
    </div>
    <div class="card-body">
      <%= render 'form', food: @food, user_id: current_user.id %>
    </div>
  </div>
</div>
