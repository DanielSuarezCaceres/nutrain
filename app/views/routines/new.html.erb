<%= simple_form_for(@routine, url: user_routines_path(user_id: params[:user_id]), wrapper: :horizontal_form) do |f| %>
    <%= f.input :name %>
    <%= f.input :description %>
 
    <%= f.button :submit, class: 'btn btn-primary' %>
<% end %>
