<div class="container p-3 col-6">
  <div class="card">
    <div class="card-header">
      <h3>Details</h3>
    </div>
    <div class="card-body">
      <%= render 'appointment' %>
      <%= link_to 'Edit', edit_user_appointment_path(@appointment), class: 'btn' %>
      <%= link_to 'Back', user_appointments_path(current_user), class: 'btn' %>
    </div>
  </div>
</div>
