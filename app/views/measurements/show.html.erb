<div class="container p-3 col-6">
  <div class="card">
    <div class="card-header">
      <h3>Details</h3>
    </div>
    <div class="card-body">
      <%= render 'measurement' %>
      <%= link_to 'Edit', edit_user_measurement_path(current_user), class: 'btn' %>
      <%= link_to 'Back', :back, class: 'btn' %>
    </div>
  </div>
</div>
