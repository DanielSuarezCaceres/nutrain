<div class="container p-2 col-6">
  <div class="card">
    <div class="card-header">
      <h2>New diet</h2>
    </div>
    <div class="card-body">
      <%= render 'form', diet: @diet, user_id: current_user.id %>
    </div>
  </div>
</div>
