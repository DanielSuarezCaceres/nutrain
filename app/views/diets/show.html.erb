<div class="container p-2">
  <div class="card">
    <div class="card-header">
      <h3>Details</h3>
    </div>
    <div class="card-body">
      <%= render 'diet' %>
      <%= link_to 'Edit', edit_user_diet_path(@diet), class: 'btn', id: 'btn-sec' %>
      <%= link_to 'Back', user_diets_path, class: 'btn', id: 'btn-sec' %>
    </div>
  </div>
</h2>
