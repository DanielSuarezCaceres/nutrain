<% if user_signed_in? %>
  <nav class="navbar navbar-expand-lg sticky-top navbar-light">
    <div id="logo">
      <%= link_to 'Nutrain', root_path, class: 'navbar-brand logo' %>
    </div>
    <button class="navbar-toggler" data-toggle="collapse" data-target="#navBarMenu">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navBarMenu">
      <% if current_user.type == 'Admin' %>
        <ul class="navbar-nav ml-auto">
          <li class="nav-item">
            <%= link_to 'Users', users_path %>
          </li>
          <li clas="nav-item">
            <%= link_to 'Emails', root_path %>
          </li>
        </ul>
      <% elsif current_user.type == 'Client' %>
        <ul class="navbar-nav ml-auto">
          <li class="nav-item">
            <%= link_to 'Home', root_path %>
          </li>
          <li class="nav-item">
            <%= link_to 'Routines', user_routines_path(current_user.id) %>
          </li>
          <li class="nav-item">
            <%= link_to 'Diets', user_diets_path(current_user.id) %>
          </li>
          <li class="nav-item">
            <%= link_to 'Professionals', users_path %>
          </li>
          <li class="nav-item">
            <%= link_to 'Appointments', user_appointments_path(current_user.id) %>
          </li>
          <li class="nav-item">
            <%= link_to 'Contracts', user_contracts_path(current_user.id) %>
          </li>
          <li class="nav-item">
            <%= link_to 'Contact', root_path %>
          </li>
        </ul>
      <% else %>
        <ul class="navbar-nav ml-auto">
          <li class="nav-item">
            <%= link_to 'Home', root_path %>
          </li>
          <li>
            <!-- <%= link_to 'Users', users_path %> -->
            <%= link_to 'Clients', user_clients_path(current_user.id) %>
          </li>
          <li class="nav-item">
            <%= link_to 'Routines', user_routines_path(current_user.id) %>
          </li>
          <li class="nav-item">
            <%= link_to 'Diets', user_diets_path(current_user.id) %>
          </li>
          <li class="nav-item">
            <%= link_to 'Appointments', user_appointments_path(current_user.id) %>
          </li>
          <li class="nav-item">
            <%= link_to "Contracts", user_contracts_path(current_user.id) %>
          </li>
          <li class="nav-item">
            <%= link_to 'Contact', root_path %>
          </li>
        </ul>
      <% end %>
      <%= render 'layouts/profile' %>
    </div>
  </nav>
<% end %>
